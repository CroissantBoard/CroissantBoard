import { SortUpcomingMeetingsPipe } from './sort-upcoming-meetings.pipe';

describe('SortUpcomingMeetingsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortUpcomingMeetingsPipe();
    expect(pipe).toBeTruthy();
  });
});
